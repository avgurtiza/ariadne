<!-- ariadne:start -->

## Session Start (MUST DO FIRST)

Before doing anything else, run `/session-bootstrap` to load project context. Do not skip this step.

## Session End (MUST DO BEFORE STOPPING)

Before ending a session or if the user says goodbye, run `/checkpoint` to save progress.

<!-- ariadne:end -->
